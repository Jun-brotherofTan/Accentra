# Accentra Backend — Python Dependencies

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6       # required for file uploads in FastAPI

# Whisper for transcription
openai-whisper>=20230918

# ML / accent model
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.35.0           # wav2vec2 + MarianMT translation
numpy>=1.24.0
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.10.0

# Pydantic (pinned for FastAPI compatibility)
pydantic>=2.0.0
